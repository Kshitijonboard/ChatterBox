<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lobby</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right, #00c6ff, #0072ff);
            color: #fff;
            text-align: center;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 30px;
            font-weight: 600;
            letter-spacing: 2px;
        }

        a {
            text-decoration: none;
        }

        button {
            background-color: #0072ff;
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 1.2rem;
            cursor: pointer;
            border-radius: 10px;
            transition: background-color 0.3s, transform 0.2s;
            letter-spacing: 1px;
        }

        button:hover {
            background-color: #005bb5;
            transform: scale(1.05);
        }

        button:active {
            transform: scale(1);
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }

            button {
                width: 80%;
                font-size: 1rem;
                padding: 12px 25px;
            }
        }
    </style>
</head>
<body>
    <div>
        <h1>Welcome to the Lobby</h1>
        <a href="/room"><button>Enter Chat Room</button></a>
    </div>
</body>
</html>
